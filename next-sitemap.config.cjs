/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://firststepcountry.com', // ← replace with your actual domain if needed
  generateRobotsTxt: true,
  sitemapSize: 5000,
}
